name: Basic Checks

on:
  pull_request:
    branches: [ main ]

jobs:
  run-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Run basic test
        run: echo "CI Pipeline is working!"
